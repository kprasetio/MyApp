/*
    This is an auto generated file and any modifications to it may result in corrupted data.
*/
define([], function() {
	var mappings = {
		"name": "name",
		"surname": "surname",
	};

	Object.freeze(mappings);

	var typings = {
		"name": "string",
		"surname": "string",
	}

	Object.freeze(typings);

	var primaryKeys = [
	];

	Object.freeze(primaryKeys);

	var config = {
		mappings: mappings,
		typings: typings,
		primaryKeys: primaryKeys,
		serviceName: "JavaPaninObjectService",
		tableName: "addExample"
	};

	Object.freeze(config);

	return config;
})