/*
    This is an auto generated file and any modifications to it may result in corrupted data.
*/
define([], function() {
	var mappings = {
		"searchTxt": "searchTxt",
	};

	Object.freeze(mappings);

	var typings = {
		"searchTxt": "string",
	}

	Object.freeze(typings);

	var primaryKeys = [
	];

	Object.freeze(primaryKeys);

	var config = {
		mappings: mappings,
		typings: typings,
		primaryKeys: primaryKeys,
		serviceName: "BestBuyObjectServices",
		tableName: "getProductsBySearchText"
	};

	Object.freeze(config);

	return config;
})